#include "main.h"
#include <string.h>
/**
 * _strlen - caluclates the length of a string
 * @s: the address of the first character of the string
 * Return: The length of the string
 */
int _strlen(char *s)
{
	int length;

	length = strlen(s);
	return (length);
}
